#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, a;
        cin >> n >> a;
        int b[n];
        for (int i = 0; i < n; i++) {
            cin >> b[i];
        }
        sort(b, b + n, greater<int>());
        

        cout << b[a - 1] - 1 << endl;
    }

    return 0;
}
